---
title: ""
format: 
  html:
    echo: false
    message: false
    warning: false
    fig-width: 10
    fig-height: 8
    fig-align: center
    dpi: 300
editor: source
---

:::{.callout-note}
Updated as of `r format(Sys.time(), '%A, %B %d, %Y at %X')`
:::

```{r setup}
#| include: false

library(tidyverse)
library(nflfastR)
library(nflreadr)
library(ggimage)
# library(plotly)
# library(ggrepel)
library(patchwork)
library(caret)
# library(vip)
# library(plotly)
# library(Rcpp)
# library(RcppParallel)
library(rsample)
# library(gbm)
# library(e1071)
# library(gtsummary)
library(reactable)

knitr::opts_chunk$set(
  fig.width = 7,
  fig.asp = .6,
  out.width = "95%"
)

theme_set(theme_minimal() + theme(legend.position = "bottom"))

# options(
#   ggplot2.continuous.colour = "viridis",
#   ggplot2.continuous.fill = "viridis"
# )
# 
# scale_colour_discrete = scale_colour_viridis_d
# scale_fill_discrete = scale_fill_viridis_d

`%nin%` = Negate(`%in%`)

set.seed(37564)

teams_colors_logos = teams_colors_logos %>% filter(team_abbr %nin% c("OAK", "SD", "STL", "LAR"))
```

```{r load data and mods}
#| include: false

week_n = ifelse(ceiling(as.integer(Sys.Date() - as.Date("2025-09-07")) / 7) > 18, 18, ceiling(as.integer(Sys.Date() - as.Date("2025-09-07")) / 7))
# week_n = 18
n_targets = round(week_n * 4, -1)
n_touches = round(week_n * 8, -1)
n_att = week_n * 15
n_rec = round(week_n * 3, -1)

# Load 2025 data
df_szn_pbp = 
  load_pbp(2025) %>%  
  filter(season_type == "REG")

df_roster = fast_scraper_roster(2025)

df_fantasy = 
  df_szn_pbp %>% 
  calculate_player_stats(weekly = T) %>% 
  mutate(
    fpts = fantasy_points + 0.5 * receptions,
    fpts_ppr = fantasy_points + 1 * receptions
  ) %>% 
  rename(posteam = recent_team) %>% 
  mutate(
    player_name = ifelse(player_name == "B.Robinson" & posteam == "ATL", "Bi.Robinson", player_name),
    player_name = ifelse(player_name == "B.Robinson" & posteam == "WAS", "Br.Robinson", player_name)
    )
```

## **Pre-Season PPR Rankings**

<!-- Download Rankings Here: [2025 Fantasy Draft Rankings 0.5PPR](fpts_rank_all_fin_2025.xlsx) -->

::: panel-tabset

### **Big Board**

```{r}
df_all_fantasy_table = readRDS(str_c("data/fantasy_data/", "df_all_fantasy_table.rds"))

make_fantasy_table <- function(
    data,
    default_sorted,
    direction = NULL,
    player_column = "player_display_name",
    team_column = "team",
    height = 700
) {
  
  # ------------------------------------------------------------
  # 1. Column names
  # ------------------------------------------------------------
  
  columns <- names(data)
  
  
  # ------------------------------------------------------------
  # 2. Create column definitions
  # ------------------------------------------------------------
  
  column_defs <- setNames(
    
    lapply(
      
      columns,
      
      function(column) {
        
        
        # ------------------------------------------------------
        # Column label
        # ------------------------------------------------------
        
        label <- labelled::var_label(
          data[[column]]
        )
        
        if (is.null(label)) {
          
          label <- column
          
        }
        
        
        # ------------------------------------------------------
        # Identify column types
        # ------------------------------------------------------
        
        is_probability <- grepl(
          "prop_",
          column,
          ignore.case = TRUE
        )
        
        is_edge <- grepl(
          "edge",
          column,
          ignore.case = TRUE
        )
        
        is_rank_adp_fin <- grepl(
          "rank_mod|adp|pred_rank_med",
          column,
          ignore.case = TRUE
        )
        
        is_feature <- !is.null(direction) &&
          column %in% names(direction)
        
        
        # ------------------------------------------------------
        # Determine color direction
        # ------------------------------------------------------
        
        column_direction <- "higher"
        
        if (is_feature) {
          
          column_direction <- direction[[column]]
          
        }
        
        
        # ------------------------------------------------------
        # Determine widths
        # ------------------------------------------------------
        
        if (column == player_column) {
          
          column_width <- 155
          
        } else if (column == team_column) {
          
          column_width <- 70
          
        } else if (is_probability) {
          
          column_width <- 85
          
        } else if (is_edge) {
          
          column_width <- 90
          
        } else if (is_rank_adp_fin) {
          
          column_width <- 85
          
        } else {
          
          column_width <- 110
          
        }
        
        
        # ------------------------------------------------------
        # Sticky columns
        # ------------------------------------------------------
        
        is_player <- column == player_column
        is_team <- column == team_column
        
        
        # ------------------------------------------------------
        # Base column definition
        # ------------------------------------------------------
        
        column_definition <- colDef(
          
          name = label,
          
          minWidth = column_width,
          
          width = column_width,
          
          sticky = if (
            is_player | is_team
          ) {
            "left"
          } else {
            NULL
          },
          
          headerStyle = list(
            whiteSpace = "normal",
            textAlign = "center",
            lineHeight = "1.2",
            padding = "8px 6px"
          )
        )
        
        
        # ------------------------------------------------------
        # Probability columns
        # ------------------------------------------------------
        
        if (is_probability) {
          
          column_definition <- colDef(
            
            name = label,
            
            format = colFormat(
              percent = TRUE,
              digits = 1
            ),
            
            style = make_style(
              data = data,
              column = column,
              direction = column_direction
            ),
            
            minWidth = column_width,
            
            width = column_width,
            
            sticky = if (
              is_player | is_team
            ) {
              "left"
            } else {
              NULL
            },
            
            headerStyle = list(
              whiteSpace = "normal",
              textAlign = "center",
              lineHeight = "1.2",
              padding = "8px 6px"
            )
          )
        }
        
        
        # ------------------------------------------------------
        # Edge columns
        # ------------------------------------------------------
        
        else if (is_edge) {
          
          column_definition <- colDef(
            
            name = label,
            
            style = make_style(
              data = data,
              column = column,
              direction = column_direction
            ),
            
            minWidth = column_width,
            
            width = column_width,
            
            sticky = if (
              is_player | is_team
            ) {
              "left"
            } else {
              NULL
            },
            
            headerStyle = list(
              whiteSpace = "normal",
              textAlign = "center",
              lineHeight = "1.2",
              padding = "8px 6px"
            )
          )
        }
        
        
        # ------------------------------------------------------
        # Feature columns
        # ------------------------------------------------------
        
        else if (is_feature) {
          
          column_definition <- colDef(
            
            name = label,
            
            style = make_style(
              data = data,
              column = column,
              direction = column_direction
            ),
            
            minWidth = column_width,
            
            width = column_width,
            
            sticky = if (
              is_player | is_team
            ) {
              "left"
            } else {
              NULL
            },
            
            headerStyle = list(
              whiteSpace = "normal",
              textAlign = "center",
              lineHeight = "1.2",
              padding = "8px 6px"
            )
          )
        }
        
        
        # ------------------------------------------------------
        # Player column
        # ------------------------------------------------------
        
        if (is_player) {
          
          column_definition <- colDef(
            
            name = label,
            
            minWidth = 150,
            width = 150,
            
            sticky = "left",
            
            style = list(
              whiteSpace = "nowrap",
              fontWeight = "bold",
              backgroundColor = "#1e1e1e"
            ),
            
            headerStyle = list(
              whiteSpace = "normal",
              textAlign = "left",
              backgroundColor = "#2b2b2b",
              color = "#ffffff",
              zIndex = 10
            )
          )
        }
        
        # Team column
        if (is_team) {
          
          column_definition <- colDef(
            
            name = label,
            
            minWidth = 70,
            width = 70,
            
            sticky = "left",
            
            style = list(
              whiteSpace = "nowrap",
              textAlign = "center",
              backgroundColor = "#1e1e1e"
            ),
            
            headerStyle = list(
              whiteSpace = "normal",
              textAlign = "center",
              backgroundColor = "#2b2b2b",
              color = "#ffffff",
              zIndex = 10
            )
          )
        }        
        
        # Return column definition
        column_definition
        
      }
    ),
    
    columns
  )
  
  
  # ------------------------------------------------------------
  # 3. Create reactable
  # ------------------------------------------------------------
  
  reactable(
    
    data,
    
    class = "fantasy-table-dark",
    
    searchable = TRUE,
    
    filterable = TRUE,
    
    sortable = TRUE,
    
    striped = FALSE,
    
    highlight = TRUE,
    
    bordered = FALSE,
    
    fullWidth = TRUE,
  
    height = height,
    
    defaultPageSize = 300,
    
    defaultSorted = default_sorted,
    
    defaultSortOrder = "asc",
    
    defaultColDef = colDef(
      
      headerStyle = list(
        whiteSpace = "normal",
        textAlign = "center",
        lineHeight = "1.2",
        padding = "8px 6px"
      ),
      
      style = list(
        whiteSpace = "normal",
        lineHeight = "1.2",
        padding = "8px 6px"
      )
    ),
    
    columns = column_defs,
  
    theme = reactableTheme(
      style = list(
        overflow = "visible"
      )
    )
  )
}

make_style <- function(
    data,
    column,
    direction = "higher"
) {
  
  values <- data[[column]]
  
  is_probability <- grepl(
    "prop_",
    column,
    ignore.case = TRUE
  )
  
  domain <- if (is_probability) {
    
    c(0, 1)
    
  } else {
    
    range(
      values,
      na.rm = TRUE
    )
  }
  
  pal <- scales::col_numeric(
    
    palette = c(
      "#d73027",
      "#fee08b",
      "#1a9850"
    ),
    
    domain = domain,
    
    reverse = direction == "lower"
  )
  
  function(value) {
    
    scaled_value <- if (
      direction == "higher"
    ) {
      
      (value - domain[1]) /
        diff(domain)
      
    } else {
      
      (domain[2] - value) /
        diff(domain)
    }
    
    list(
      
      backgroundColor = pal(value),
      
      color = ifelse(
        scaled_value > 0.70,
        "white",
        "black"
      ),
      
      fontWeight = "bold"
    )
  }
}

make_fantasy_table(
  
  data = df_all_fantasy_table,
  
  default_sorted = "rank_vor",
  
  height = 700
)
```

### **QB**

```{r qb tbl}
# df_fpts_qb_test = readRDS(str_c("data/fantasy_data/", "df_fpts_qb_test.rds"))

df_qb_fantasy_table = readRDS(str_c("data/fantasy_data/", "df_qb_fantasy_table.rds"))

make_fantasy_table(
  
  data = df_qb_fantasy_table,
  
  default_sorted = "rank_mod",
  
  height = 700
)


# df_qb_features_table = 
#   df_fpts_qb_test %>% 
#   left_join(
#     df_qb_fantasy_table %>% 
#       select(player_display_name, rank_mod, pred_fpts),
#     by = "player_display_name"
#   ) %>% 
#   mutate(
#     across(
#       c(starts_with("prior_"), games_missed_rate, expect_rush, ol_cont_grade_pass),
#       ~ round(., 1)
#     )
#   ) %>% 
#   select(player_display_name, team, rank_mod, pred_fpts, everything(), -player_id) %>% 
#   labelled::set_variable_labels(
#     "player_display_name" = "Player", 
#     "team" = "Team", 
#     "rank_mod" = "Rank", 
#     "pred_fpts" = "Projection", 
#     "prior_fpts" = "Weighted Prior FPTS", 
#     "prior_adot" = "Weighted Prior ADOT", 
#     "prior_qb_epa" = "Weighted Prior EPA", 
#     "draft_number" = "Draft Number", 
#     "prior_fpts_other" = "QB Competition", 
#     "qb_new_team" = "New Team", 
#     "games_missed_rate" = "Missed Games Rate", 
#     "expect_rush" = "Expected Rushes", 
#     "ol_cont_grade_pass" = "Returning OL Grade"
#   )
# 
# view(df_qb_features_table)
# 
# proj_pal_qb =
#   scales::col_numeric(
#     palette = c("#d73027", "#fee08b", "#1a9850"),
#     domain = c(min(df_qb_features_table$pred_fpts), max(df_qb_features_table$pred_fpts))
#   )
# 
# prior_pal_qb =
#   scales::col_numeric(
#     palette = c("#d73027", "#fee08b", "#1a9850"),
#     domain = c(min(df_qb_features_table$prior_fpts), max(df_qb_features_table$prior_fpts))
#   )
# 
# adot_pal_qb =
#   scales::col_numeric(
#     palette = c("#d73027", "#fee08b", "#1a9850"),
#     domain = c(min(df_qb_features_table$prior_adot), max(df_qb_features_table$prior_adot))
#   )
# 
# epa_pal_qb =
#   scales::col_numeric(
#     palette = c("#d73027", "#fee08b", "#1a9850"),
#     domain = c(min(df_qb_features_table$prior_qb_epa), max(df_qb_features_table$prior_qb_epa))
#   )
# 
# fpts_other_pal_qb =
#   scales::col_numeric(
#     palette = c("#1a9850", "#fee08b", "#d73027"),
#     domain = c(min(df_qb_features_table$prior_fpts_other), max(df_qb_features_table$prior_fpts_other))
#   )
# 
# games_missed_pal_qb =
#   scales::col_numeric(
#     palette = c("#1a9850", "#fee08b", "#d73027"),
#     domain = c(min(df_qb_features_table$games_missed_rate), max(df_qb_features_table$games_missed_rate))
#   )
# 
# rush_pal_qb =
#   scales::col_numeric(
#     palette = c("#d73027", "#fee08b", "#1a9850"),
#     domain = c(min(df_qb_features_table$expect_rush), max(df_qb_features_table$expect_rush))
#   )
# 
# draft_pal_qb =
#   scales::col_numeric(
#     palette = c("#1a9850", "#fee08b", "#d73027"),
#     domain = c(max(df_qb_features_table$draft_number), min(df_qb_features_table$draft_number))
#   )
# 
# ol_pal_qb =
#   scales::col_numeric(
#     palette = c("#d73027", "#fee08b", "#1a9850"),
#     domain = c(min(df_qb_features_table$ol_cont_grade_pass), max(df_qb_features_table$ol_cont_grade_pass))
#   )
# 
# new_team_pal_qb = 
#   scales::col_numeric(
#     palette = c("#1a9850", "#fee08b", "#d73027"),
#     domain = c(min(df_qb_features_table$qb_new_team), max(df_qb_features_table$qb_new_team))
#   )
# 
# reactable(
#   df_qb_features_table,
#   searchable = TRUE,
#   filterable = TRUE,
#   sortable = TRUE,
#   striped = TRUE,
#   highlight = TRUE,
#   bordered = TRUE,
#   defaultPageSize = 100,
#   defaultSorted = "qb_rank_svm",
#   defaultSortOrder = "asc",
#   columns = list(
# 
#     player_display_name = colDef(
#       name = "Player",
#       minWidth = 100
#     ),
# 
#     team = colDef(
#       name = "Team",
#       minWidth = 45
#     ),
# 
#     qb_rank_svm = colDef(
#       name = "Rank",
#       minWidth = 45
#     ),
# 
#     pred_fpts = colDef(
#       name = "Projection",
#       style = function(value) {
#       
#         text_color <- ifelse(value > quantile(df_qb_features_table$pred_fpts, 0.6)[[1]], "white", "black")
#       
#         list(
#           background = proj_pal_qb(value),
#           color = text_color,
#           fontWeight = "bold"
#         )
#       },
#       minWidth = 60
#     ),
#     
#     prior_fpts = colDef(
#       name = "Weighted Prior FPTS",
#       style = function(value) {
#       
#         text_color <- ifelse(value > quantile(df_qb_features_table$prior_fpts, 0.6)[[1]], "white", "black")
#       
#         list(
#           background = prior_pal_qb(value),
#           color = text_color,
#           fontWeight = "bold"
#         )
#       },
#       minWidth = 60
#     ),
#     
#     prior_adot = colDef(
#       name = "Weighted Prior ADOT",
#       style = function(value) {
#       
#         text_color <- ifelse(value > quantile(df_qb_features_table$prior_adot, 0.6)[[1]], "white", "black")
#       
#         list(
#           background = adot_pal_qb(value),
#           color = text_color,
#           fontWeight = "bold"
#         )
#       },
#       minWidth = 60
#     ),
#     
#     prior_qb_epa = colDef(
#       name = "Weighted Prior EPA",
#       style = function(value) {
#       
#         text_color <- ifelse(value > quantile(df_qb_features_table$prior_qb_epa, 0.6)[[1]], "white", "black")
#       
#         list(
#           background = epa_pal_qb(value),
#           color = text_color,
#           fontWeight = "bold"
#         )
#       },
#       minWidth = 60
#     ),
#     
#     draft_number = colDef(
#       name = "Draft Number",
#       style = function(value) {
# 
#         text_color <- ifelse(value >  quantile(df_qb_features_table$draft_number, 0.6)[[1]], "white", "black")
# 
#         list(
#           background = draft_pal_qb(value),
#           color = text_color,
#           fontWeight = "bold"
#         )
#       },
#       minWidth = 60
#     ),
#     
#     prior_fpts_other = colDef(
#       name = "QB Competition",
#       style = function(value) {
#       
#         text_color <- ifelse(value > quantile(df_qb_features_table$prior_fpts_other, 0.6)[[1]], "white", "black")
#       
#         list(
#           background = fpts_other_pal_qb(value),
#           color = text_color,
#           fontWeight = "bold"
#         )
#       },
#       minWidth = 60
#     ),
#     
#     qb_new_team = colDef(
#       name = "New Team",
#       style = function(value) {
# 
#         text_color <- ifelse(value > 0.6, "white", "black")
# 
#         list(
#           background = new_team_pal_qb(value),
#           color = text_color,
#           fontWeight = "bold"
#         )
#       },
#       minWidth = 60
#     ),
#     
#     games_missed_rate = colDef(
#       name = "Missed Games Rate",
#       style = function(value) {
#       
#         text_color <- ifelse(value > quantile(df_qb_features_table$games_missed_rate, 0.6)[[1]], "white", "black")
#       
#         list(
#           background = games_missed_pal_qb(value),
#           color = text_color,
#           fontWeight = "bold"
#         )
#       },
#       minWidth = 60
#     ),
#     
#     expect_rush = colDef(
#       name = "Expected Rushes",
#       style = function(value) {
#       
#         text_color <- ifelse(value > quantile(df_qb_features_table$expect_rush, 0.6)[[1]], "white", "black")
#       
#         list(
#           background = rush_pal_qb(value),
#           color = text_color,
#           fontWeight = "bold"
#         )
#       },
#       minWidth = 60
#     ),
#     
#     ol_cont_grade_pass = colDef(
#       name = "OL Grades",
#       style = function(value) {
#       
#         text_color <- ifelse(value > quantile(df_qb_features_table$ol_cont_grade_pass, 0.6)[[1]], "white", "black")
#       
#         list(
#           background = ol_pal_qb(value),
#           color = text_color,
#           fontWeight = "bold"
#         )
#       },
#       minWidth = 60
#     )
#     
#   )
# )

# make_style <- function(data, column) {
#   
#   # Determine whether column is a probability variable
#   is_probability <- grepl(
#     "^prop_",
#     column,
#     ignore.case = TRUE
#   )
#   
#   # Automatically determine palette domain
#   domain <- if (is_probability) {
#     c(0, 1)
#   } else {
#     range(data[[column]], na.rm = TRUE)
#   }
#   
#   # Create palette
#   pal <- scales::col_numeric(
#     palette = c(
#       "#d73027",
#       "#fee08b",
#       "#1a9850"
#     ),
#     domain = domain
#   )
#   
#   # Return style function
#   function(value) {
#     
#     # 70% through the color scale
#     text_cutoff <- domain[1] + 0.70 * diff(domain)
#     
#     list(
#       background = pal(value),
#       color = ifelse(
#         value > text_cutoff,
#         "white",
#         "black"
#       ),
#       fontWeight = "bold"
#     )
#   }
# }
# 
# make_fantasy_table <- function(
#     data,
#     default_sorted
# ) {
#   
#   columns <- names(data)
#   
#   column_defs <- setNames(
#     lapply(columns, function(column) {
#       
#       # Get the label directly from the data
#       label <- labelled::var_label(data[[column]])
#       
#       # Fall back to the column name if no label exists
#       if (is.null(label)) {
#         label <- column
#       }
#       
#       # Identify probability columns
#       is_probability <- grepl(
#         "^(p_|prop_)",
#         column,
#         ignore.case = TRUE
#       )
#       
#       # Identify columns to color
#       is_edge <- grepl(
#         "edge",
#         column,
#         ignore.case = TRUE
#       )
#       
#       # Probability columns
#       if (is_probability) {
#         
#         colDef(
#           name = label,
#           format = colFormat(
#             percent = TRUE,
#             digits = 1
#           ),
#           style = make_style(
#             data,
#             column
#           ),
#           minWidth = 60
#         )
#         
#       # Edge columns
#       } else if (is_edge) {
#         
#         colDef(
#           name = label,
#           style = make_style(
#             data,
#             column
#           ),
#           minWidth = 60
#         )
#         
#       # All other columns
#       } else {
#         
#         colDef(
#           name = label,
#           minWidth = 60
#         )
#       }
#       
#     }),
#     columns
#   )
#   
#   reactable(
#     data,
#     searchable = TRUE,
#     filterable = TRUE,
#     sortable = TRUE,
#     striped = TRUE,
#     highlight = TRUE,
#     bordered = TRUE,
#     defaultPageSize = 100,
#     defaultSorted = default_sorted,
#     defaultSortOrder = "asc",
#     columns = column_defs
#   )
# }
# 
# make_fantasy_table(
#   df_qb_fantasy_table,
#   default_sorted = "rank_mod"
# )
```

### **RB**

```{r rb tbl}
df_rb_fantasy_table = readRDS(str_c("data/fantasy_data/", "df_rb_fantasy_table.rds"))

# make_style <- function(data, column) {
#   
#   # Determine whether column is a probability variable
#   is_probability <- grepl(
#     "^prop_",
#     column,
#     ignore.case = TRUE
#   )
#   
#   # Automatically determine palette domain
#   domain <- if (is_probability) {
#     c(0, 1)
#   } else {
#     range(data[[column]], na.rm = TRUE)
#   }
#   
#   # Create palette
#   pal <- scales::col_numeric(
#     palette = c(
#       "#d73027",
#       "#fee08b",
#       "#1a9850"
#     ),
#     domain = domain
#   )
#   
#   # Return style function
#   function(value) {
#     
#     # 70% through the color scale
#     text_cutoff <- domain[1] + 0.70 * diff(domain)
#     
#     list(
#       background = pal(value),
#       color = ifelse(
#         value > text_cutoff,
#         "white",
#         "black"
#       ),
#       fontWeight = "bold"
#     )
#   }
# }
# 
# make_fantasy_table <- function(
#     data,
#     default_sorted
# ) {
#   
#   columns <- names(data)
#   
#   column_defs <- setNames(
#     lapply(columns, function(column) {
#       
#       # Get the label directly from the data
#       label <- labelled::var_label(data[[column]])
#       
#       # Fall back to the column name if no label exists
#       if (is.null(label)) {
#         label <- column
#       }
#       
#       # Identify probability columns
#       is_probability <- grepl(
#         "^(p_|prop_)",
#         column,
#         ignore.case = TRUE
#       )
#       
#       # Identify columns to color
#       is_edge <- grepl(
#         "edge",
#         column,
#         ignore.case = TRUE
#       )
#       
#       # Probability columns
#       if (is_probability) {
#         
#         colDef(
#           name = label,
#           format = colFormat(
#             percent = TRUE,
#             digits = 1
#           ),
#           style = make_style(
#             data,
#             column
#           ),
#           minWidth = 60
#         )
#         
#       # Edge columns
#       } else if (is_edge) {
#         
#         colDef(
#           name = label,
#           style = make_style(
#             data,
#             column
#           ),
#           minWidth = 60
#         )
#         
#       # All other columns
#       } else {
#         
#         colDef(
#           name = label,
#           minWidth = 60
#         )
#       }
#       
#     }),
#     columns
#   )
#   
#   reactable(
#     data,
#     searchable = TRUE,
#     filterable = TRUE,
#     sortable = TRUE,
#     striped = TRUE,
#     highlight = TRUE,
#     bordered = TRUE,
#     defaultPageSize = 100,
#     defaultSorted = default_sorted,
#     defaultSortOrder = "asc",
#     columns = column_defs
#   )
# }

make_fantasy_table(
  
  df_rb_fantasy_table,
  
  default_sorted = "rank_mod",
  
  height = 700
  
)
```

### **WR**

```{r wr tbl}
df_wr_fantasy_table = readRDS(str_c("data/fantasy_data/", "df_wr_fantasy_table.rds"))

# make_style <- function(data, column) {
#   
#   # Determine whether column is a probability variable
#   is_probability <- grepl(
#     "^prop_",
#     column,
#     ignore.case = TRUE
#   )
#   
#   # Automatically determine palette domain
#   domain <- if (is_probability) {
#     c(0, 1)
#   } else {
#     range(data[[column]], na.rm = TRUE)
#   }
#   
#   # Create palette
#   pal <- scales::col_numeric(
#     palette = c(
#       "#d73027",
#       "#fee08b",
#       "#1a9850"
#     ),
#     domain = domain
#   )
#   
#   # Return style function
#   function(value) {
#     
#     # 70% through the color scale
#     text_cutoff <- domain[1] + 0.70 * diff(domain)
#     
#     list(
#       background = pal(value),
#       color = ifelse(
#         value > text_cutoff,
#         "white",
#         "black"
#       ),
#       fontWeight = "bold"
#     )
#   }
# }
# 
# make_fantasy_table <- function(
#     data,
#     default_sorted
# ) {
#   
#   columns <- names(data)
#   
#   column_defs <- setNames(
#     lapply(columns, function(column) {
#       
#       # Get the label directly from the data
#       label <- labelled::var_label(data[[column]])
#       
#       # Fall back to the column name if no label exists
#       if (is.null(label)) {
#         label <- column
#       }
#       
#       # Identify probability columns
#       is_probability <- grepl(
#         "^(p_|prop_)",
#         column,
#         ignore.case = TRUE
#       )
#       
#       # Identify columns to color
#       is_edge <- grepl(
#         "edge",
#         column,
#         ignore.case = TRUE
#       )
#       
#       # Probability columns
#       if (is_probability) {
#         
#         colDef(
#           name = label,
#           format = colFormat(
#             percent = TRUE,
#             digits = 1
#           ),
#           style = make_style(
#             data,
#             column
#           ),
#           minWidth = 60
#         )
#         
#       # Edge columns
#       } else if (is_edge) {
#         
#         colDef(
#           name = label,
#           style = make_style(
#             data,
#             column
#           ),
#           minWidth = 60
#         )
#         
#       # All other columns
#       } else {
#         
#         colDef(
#           name = label,
#           minWidth = 60
#         )
#       }
#       
#     }),
#     columns
#   )
#   
#   reactable(
#     data,
#     searchable = TRUE,
#     filterable = TRUE,
#     sortable = TRUE,
#     striped = TRUE,
#     highlight = TRUE,
#     bordered = TRUE,
#     defaultPageSize = 100,
#     defaultSorted = default_sorted,
#     defaultSortOrder = "asc",
#     columns = column_defs
#   )
# }

make_fantasy_table(
  
  df_wr_fantasy_table,
  
  default_sorted = "rank_mod",
  
  height = 700
  
)
```

### **TE**

```{r te tbl}
df_te_fantasy_table = readRDS(str_c("data/fantasy_data/", "df_te_fantasy_table.rds"))

# make_style <- function(data, column) {
#   
#   # Determine whether column is a probability variable
#   is_probability <- grepl(
#     "^prop_",
#     column,
#     ignore.case = TRUE
#   )
#   
#   # Automatically determine palette domain
#   domain <- if (is_probability) {
#     c(0, 1)
#   } else {
#     range(data[[column]], na.rm = TRUE)
#   }
#   
#   # Create palette
#   pal <- scales::col_numeric(
#     palette = c(
#       "#d73027",
#       "#fee08b",
#       "#1a9850"
#     ),
#     domain = domain
#   )
#   
#   # Return style function
#   function(value) {
#     
#     # 70% through the color scale
#     text_cutoff <- domain[1] + 0.70 * diff(domain)
#     
#     list(
#       background = pal(value),
#       color = ifelse(
#         value > text_cutoff,
#         "white",
#         "black"
#       ),
#       fontWeight = "bold"
#     )
#   }
# }
# 
# make_fantasy_table <- function(
#     data,
#     default_sorted
# ) {
#   
#   columns <- names(data)
#   
#   column_defs <- setNames(
#     lapply(columns, function(column) {
#       
#       # Get the label directly from the data
#       label <- labelled::var_label(data[[column]])
#       
#       # Fall back to the column name if no label exists
#       if (is.null(label)) {
#         label <- column
#       }
#       
#       # Identify probability columns
#       is_probability <- grepl(
#         "^(p_|prop_)",
#         column,
#         ignore.case = TRUE
#       )
#       
#       # Identify columns to color
#       is_edge <- grepl(
#         "edge",
#         column,
#         ignore.case = TRUE
#       )
#       
#       # Probability columns
#       if (is_probability) {
#         
#         colDef(
#           name = label,
#           format = colFormat(
#             percent = TRUE,
#             digits = 1
#           ),
#           style = make_style(
#             data,
#             column
#           ),
#           minWidth = 60
#         )
#         
#       # Edge columns
#       } else if (is_edge) {
#         
#         colDef(
#           name = label,
#           style = make_style(
#             data,
#             column
#           ),
#           minWidth = 60
#         )
#         
#       # All other columns
#       } else {
#         
#         colDef(
#           name = label,
#           minWidth = 60
#         )
#       }
#       
#     }),
#     columns
#   )
#   
#   reactable(
#     data,
#     searchable = TRUE,
#     filterable = TRUE,
#     sortable = TRUE,
#     striped = TRUE,
#     highlight = TRUE,
#     bordered = TRUE,
#     defaultPageSize = 100,
#     defaultSorted = default_sorted,
#     defaultSortOrder = "asc",
#     columns = column_defs
#   )
# }

make_fantasy_table(

  df_te_fantasy_table,
  
  default_sorted = "rank_mod",
  
  height = 700
  
)
```

:::

## **Fantasy Football Stats (2025)**

::: panel-tabset

### **PPR**

::: panel-tabset

#### **QB**

```{r fan qb ppr}
df_qb_att = 
  df_szn_pbp %>% 
  filter(pass_attempt == 1 & sack == 0) %>% 
  group_by(posteam, passer) %>% 
  summarize(n_att = n()) %>% 
  drop_na(passer) %>% 
  rename(player_name = passer)

df_qb_fpts = 
  df_fantasy %>% 
  left_join(df_qb_att) %>% 
  filter(n_att >= n_att) %>% 
  inner_join(
    fast_scraper_roster(2025) %>% 
    filter(position == "QB") %>% 
    select(gsis_id),
      by = c("player_id" = "gsis_id")
  ) %>% 
  select(posteam, week, player_name, player_display_name, fpts)

df_med_qb = 
  df_qb_fpts %>% 
  group_by(player_display_name, posteam) %>% 
  summarize(med_pts = median(fpts)) %>% 
  arrange(-med_pts) %>% 
  rowid_to_column("rank") %>% 
  filter(rank <= 20) %>% 
  mutate(player_name_og = player_display_name) %>% 
  unite("rank_name", rank, player_display_name, sep = ". ") %>% 
  arrange(med_pts) %>% 
  rename(player_display_name = player_name_og)

df_qb_fpts = 
  df_qb_fpts %>% 
  left_join(df_med_qb) %>% 
  group_by(med_pts, posteam) %>% 
  mutate(rank_name = fct_reorder(as.factor(rank_name), med_pts))

df_qb_fpts_cols = 
  teams_colors_logos %>% 
  mutate(posteam = team_abbr) %>% 
  left_join(df_med_qb) %>% 
  group_by(med_pts, posteam) %>% 
  mutate(player_display_name = fct_reorder(as.factor(player_display_name), med_pts)) %>% 
  arrange(med_pts) %>% 
  drop_na(player_display_name)

df_qb_fpts %>% 
  drop_na() %>% 
  ggplot(aes(x = fpts, y = rank_name)) +
  geom_boxplot(fill = df_qb_fpts_cols$team_color, alpha = 0.75) +
  labs(
    title = glue::glue("2025 QB PPG (Min. {n_att} Passing Attempts)"), 
    x = "Fantasy PPG", 
    y = "QB"
  ) + 
  theme(
    plot.title = element_text(hjust = 0.5),
    axis.text.y = element_text(size = 8)
  )
```

#### **RB**

```{r rb fan ppr}
df_rb_att = 
  df_szn_pbp %>% 
  filter(rush_attempt == 1) %>% 
  group_by(posteam, rusher) %>% 
  summarize(n_att = n()) %>% 
  drop_na(rusher) %>% 
  rename(player_name = rusher) %>% 
  mutate(
    player_name = ifelse(player_name == "B.Robinson" & posteam == "ATL", "Bi.Robinson", player_name),
    player_name = ifelse(player_name == "B.Robinson" & posteam == "WAS", "Br.Robinson", player_name)
  )

df_rb_fpts = 
  df_fantasy %>% 
  group_by(player_id) %>% 
  mutate(n_rec = sum(receptions)) %>% 
  ungroup() %>% 
  left_join(df_rb_att) %>% 
  mutate(
    touches = n_att + n_rec
  ) %>% 
  filter(touches >= n_touches) %>% 
  inner_join(
    fast_scraper_roster(2025) %>% 
    filter(position == "RB") %>% 
    select(gsis_id),
      by = c("player_id" = "gsis_id")
  ) %>% 
  select(posteam, week, player_name, player_display_name, fpts, fpts_ppr, touches)

df_med_rb = 
  df_rb_fpts %>% 
  group_by(player_display_name, posteam, touches) %>% 
  summarize(med_pts = median(fpts)) %>% 
  arrange(-med_pts, -touches) %>% 
  rowid_to_column("rank") %>% 
  filter(rank <= 40) %>% 
  mutate(
    player_name_og = player_display_name,
    rank_og = rank
  ) %>% 
  unite("rank_name", rank, player_display_name, sep = ". ") %>% 
  arrange(med_pts, touches) %>% 
  rename(player_display_name = player_name_og)

df_med_rb_ppr = 
  df_rb_fpts %>% 
  group_by(player_display_name, posteam, touches) %>% 
  summarize(med_pts = median(fpts_ppr)) %>% 
  arrange(-med_pts, -touches) %>% 
  rowid_to_column("rank") %>% 
  filter(rank <= 40) %>% 
  mutate(
    player_name_og = player_display_name,
    rank_og = rank
  ) %>% 
  unite("rank_name", rank, player_display_name, sep = ". ") %>% 
  arrange(med_pts, touches) %>% 
  rename(player_display_name = player_name_og)

# rb_fpts = 
#   rb_fpts %>% 
#   left_join(med_rb) %>% 
#   group_by(med_pts, posteam) %>% 
#   mutate(rank_name = fct_reorder(as.factor(rank_name), med_pts))

df_rb_fpts_cols = 
  teams_colors_logos %>% 
  mutate(posteam = team_abbr) %>% 
  left_join(df_med_rb, by = "posteam") %>% 
  group_by(med_pts, posteam) %>% 
  mutate(player_display_name = fct_reorder(as.factor(player_display_name), med_pts)) %>% 
  arrange(med_pts, touches) %>% 
  drop_na(player_display_name)

df_rb_fpts_ppr_cols = 
  teams_colors_logos %>% 
  mutate(posteam = team_abbr) %>% 
  left_join(df_med_rb_ppr, by = "posteam") %>% 
  group_by(med_pts, posteam) %>% 
  mutate(player_display_name = fct_reorder(as.factor(player_display_name), med_pts)) %>% 
  arrange(med_pts, touches) %>% 
  drop_na(player_display_name)

df_rb_fpts %>% 
  left_join(df_med_rb_ppr) %>% 
  drop_na() %>% 
  ggplot(aes(x = fpts_ppr, y = fct_reorder(as.factor(rank_name), -rank_og))) +
  geom_boxplot(fill = df_rb_fpts_ppr_cols$team_color, alpha = 0.75) +
  labs(
    title = glue::glue("2025 RB PPG (Min. {n_touches} Touches)"),     
    x = "PPR Fantasy PPG", 
    y = "RB"
  ) + 
  theme(
    plot.title = element_text(hjust = 0.5),
    axis.text.y = element_text(size = 8)
  )

```

#### **WR**

```{r wr fan ppr}
df_wr_tar = 
  df_roster %>% 
  filter(position == "WR") %>% 
  mutate(receiver_id = gsis_id) %>% 
  select(receiver_id, position) %>% 
  inner_join(df_szn_pbp, by = "receiver_id") %>% 
  filter(pass_attempt == 1) %>% 
  rename(team_abbr = posteam) %>% 
  group_by(team_abbr, receiver) %>% 
  summarize(
    targets = n()#,
    #mean_epa = mean(epa)
  ) %>% 
  filter(receiver %nin% NA) %>% 
  ungroup()

df_wr_epa = 
  df_roster %>% 
  filter(position == "WR") %>% 
  mutate(receiver_id = gsis_id) %>% 
  select(receiver_id, position) %>% 
  inner_join(df_szn_pbp, by = "receiver_id") %>% 
  filter(complete_pass == 1) %>% 
  rename(team_abbr = posteam) %>% 
  group_by(team_abbr, receiver) %>% 
  summarize(
    epa = epa,
    mean_epa = mean(epa),
    med_epa = median(epa),
    n_rec = n()
  ) %>% 
  filter(receiver %nin% NA) %>% 
  ungroup() %>% 
  left_join(df_wr_tar) %>% 
  drop_na(targets) %>% 
  mutate(receiver = fct_reorder(receiver, epa, median))

df_wr_epa_cols = 
  df_wr_epa %>% 
  group_by(team_abbr, receiver) %>% 
  summarize(
    med_epa = median(epa)
  ) %>% 
  left_join(teams_colors_logos, by = "team_abbr") %>% 
  group_by(med_epa, team_abbr) %>% 
  mutate(receiver = fct_reorder(as.factor(receiver), med_epa)) %>% 
  arrange(med_epa) %>% 
  drop_na(receiver)

df_fantasy = 
  df_szn_pbp %>% 
  calculate_player_stats(weekly = T) %>% 
  mutate(
    fpts = fantasy_points + 0.5 * receptions,
    fpts_ppr = fantasy_points + 1 * receptions
  ) %>% 
  rename(posteam = recent_team) %>% 
  mutate(
    player_name = ifelse(player_name == "B.Robinson" & posteam == "ATL", "Bi.Robinson", player_name),
    player_name = ifelse(player_name == "B.Robinson" & posteam == "WAS", "Br.Robinson", player_name)
    )

# WR
df_wr_rec = 
  df_szn_pbp %>% 
  filter(complete_pass == 1) %>% 
  group_by(posteam, receiver) %>% 
  summarize(n_rec = n()) %>% 
  drop_na(receiver) %>% 
  rename(player_name = receiver) %>% 
  mutate(
    player_name = ifelse(player_name == "B.Robinson" & posteam == "ATL", "Bi.Robinson", player_name),
    player_name = ifelse(player_name == "B.Robinson" & posteam == "WAS", "Br.Robinson", player_name)
  )

df_wr_fpts = 
  df_fantasy %>% 
  group_by(posteam, player_name, player_id) %>% 
  mutate(targets = sum(targets)) %>% 
  filter(targets >= n_targets) %>% 
  left_join(df_wr_rec) %>% 
  inner_join(
    fast_scraper_roster(2025) %>% 
    filter(position == "WR") %>% 
    select(gsis_id),
      by = c("player_id" = "gsis_id")
  ) %>% 
  select(posteam, week, player_name, player_display_name, player_id, fpts, fpts_ppr, targets)

df_med_wr = 
  df_wr_fpts %>% 
  group_by(player_display_name, posteam, targets) %>% 
  summarize(med_pts = median(fpts)) %>%  
  arrange(-med_pts, -targets) %>% 
  rowid_to_column("rank") %>% 
  filter(rank <= 40) %>% 
  mutate(
    player_name_og = player_display_name,
    rank_og = rank
  ) %>% 
  unite("rank_name", rank, player_display_name, sep = ". ") %>% 
  arrange(med_pts, targets) %>% 
  rename(player_display_name = player_name_og) %>% 
  ungroup()
  
df_med_wr_ppr = 
  df_wr_fpts %>% 
  group_by(player_display_name, posteam, targets) %>% 
  summarize(med_pts = median(fpts_ppr)) %>%  
  arrange(-med_pts, -targets) %>% 
  rowid_to_column("rank") %>% 
  filter(rank <= 40) %>% 
  mutate(
    player_name_og = player_display_name,
    rank_og = rank
  ) %>% 
  unite("rank_name", rank, player_display_name, sep = ". ") %>% 
  arrange(med_pts, targets) %>% 
  rename(player_display_name = player_name_og) %>% 
  ungroup()

df_wr_fpts_cols = 
  teams_colors_logos %>% 
  mutate(posteam = team_abbr) %>% 
  left_join(df_med_wr, by = "posteam") %>% 
  # right_join(wr_fpts) %>% 
  group_by(med_pts, posteam) %>% 
  # mutate(player_name = fct_reorder(as.factor(player_name), med_pts)) %>% 
  arrange(med_pts, targets) %>% 
  drop_na(player_display_name)

df_wr_fpts_ppr_cols = 
  teams_colors_logos %>% 
  mutate(posteam = team_abbr) %>% 
  left_join(df_med_wr_ppr, by = "posteam") %>% 
  # right_join(wr_fpts) %>% 
  group_by(med_pts, posteam) %>% 
  # mutate(player_name = fct_reorder(as.factor(player_name), med_pts)) %>% 
  arrange(med_pts, targets) %>% 
  drop_na(player_display_name)

df_wr_fpts %>% 
  left_join(df_med_wr_ppr) %>% 
  left_join(df_wr_fpts_cols %>% select(player_display_name, team_color), by = "player_display_name") %>% 
  drop_na() %>% 
  # mutate(rank_name = ) %>% 
  # left_join(wr_fpts_cols %>% select(player_display_name, team_color)) %>% 
  ggplot(aes(x = fpts_ppr, y = fct_reorder(as.factor(rank_name), -rank_og))) +
  geom_boxplot(aes(fill = team_color), alpha = 0.75) +
  scale_fill_identity() +
  labs(
    title = glue::glue("2025 WR PPG (Min. {n_targets} Targets)"), 
    x = "PPR Fantasy PPG", 
    y = "WR"
  ) + 
  theme(
    plot.title = element_text(hjust = 0.5),
    axis.text.y = element_text(size = 8)
  )

df_wr_mean_fpts = 
  df_wr_fpts %>% 
  group_by(posteam, player_name) %>% 
  mutate(sum_pts = sum(fpts), n_weeks = n()) %>% 
  distinct(posteam, player_name, sum_pts, targets, n_weeks) %>% 
  ungroup() %>% 
  mutate(
    mean_pts = sum_pts / n_weeks, 
    mean_tar = targets / n_weeks
  ) %>% 
  left_join(df_med_wr) %>% 
  filter(as.integer(str_extract(rank_name, "^[0-9]+")) <= 40)

# PPG by Targets

# wr_fpts %>% 
#   left_join(med_wr) %>% 
#   drop_na() %>% 
#   ungroup() %>% 
#   group_by(posteam, player_name) %>% 
#   mutate(n_weeks = n()) %>% 
#   ungroup() %>% 
#   distinct(posteam, player_name, med_pts, targets, n_weeks) %>% 
#   mutate(tar_gm = targets / n_weeks) %>% 
#   # left_join(wr_fpts_cols %>% select(player_name, team_color)) %>% 
#   arrange(med_pts, player_name) %>% 
#   ggplot(aes(x = tar_gm, y = med_pts)) +
#   geom_point(color = wr_fpts_cols$team_color, alpha = 0.75, size = 3) +
#   geom_text_repel(aes(label = player_name), alpha = 4) +
#   geom_hline(yintercept = mean(wr_mean_fpts$mean_pts), color = "black", linetype = "dashed") + 
#   geom_vline(xintercept = mean(wr_mean_fpts$mean_tar), color = "black", linetype = "dashed") + 
#   geom_rect(aes(xmin = -Inf, xmax = Inf, ymin = mean(wr_mean_fpts$mean_pts) - qnorm(0.975)*sd(wr_mean_fpts$mean_pts)/sqrt(length(wr_mean_fpts$mean_pts)), ymax = mean(wr_mean_fpts$mean_pts) + qnorm(0.975)*sd(wr_mean_fpts$mean_pts)/sqrt(length(wr_mean_fpts$mean_pts))), alpha = 0.005, fill = "yellow") + 
#   geom_rect(aes(ymin = -Inf, ymax = Inf, xmin = mean(wr_mean_fpts$mean_tar) - qnorm(0.975)*sd(wr_mean_fpts$mean_tar)/sqrt(length(wr_mean_fpts$mean_tar)), xmax = mean(wr_mean_fpts$mean_tar) + qnorm(0.975)*sd(wr_mean_fpts$mean_tar)/sqrt(length(wr_mean_fpts$mean_tar))), alpha = 0.005, fill = "yellow") + 
#   scale_y_continuous(breaks = seq(0, 25, by = 5)) +
#   # geom_smooth() +
#   labs(
#     title = "2025 WR Median Fantasy PPG vs Targets/Game (Min. 40 Targets)",
#     x = "Targets/Game",
#     y = "Median Fantasy PPG"
#   ) + 
#   theme(
#     legend.position = "none",
#     plot.title = element_text(hjust = 0.42)
#   )
# 
# # Fantasy PPG and EPA
# wr_fpts %>% 
#   left_join(med_wr) %>% 
#   drop_na() %>% 
#   left_join(
#     wr_epa %>% 
#       distinct(team_abbr, receiver, mean_epa) %>% 
#       rename(
#         posteam = team_abbr,
#         player_name = receiver
#       ),
#     by = c("posteam", "player_name")
#   ) %>% 
#   distinct(posteam, player_name, mean_epa, med_pts, targets) %>% 
#   arrange(med_pts) %>% 
#   ggplot(aes(x = mean_epa, y = med_pts)) +
#   geom_point(aes(size = targets / 10), color = wr_fpts_cols$team_color, alpha = 0.75) +
#   geom_text_repel(aes(label = player_name), alpha = 4) + 
#   geom_smooth() +
#   labs(
#     title = "2025 WR Median Fantasy PPG vs EPA/Catch (Min. 40 Targets)",
#     x = "Mean EPA/Catch",
#     y = "Median Fantasy PPG"
#   ) + 
#   theme(
#     legend.position = "none",
#     plot.title = element_text(hjust = 0.42)
#   )
# 
# # Fantasy PPG and EPA
# wr_fpts %>% 
#   left_join(med_wr) %>% 
#   left_join(wr_adot) %>% 
#   drop_na() %>% 
#   left_join(
#     wr_epa %>% 
#       distinct(team_abbr, receiver, mean_epa) %>% 
#       rename(
#         posteam = team_abbr,
#         player_name = receiver
#       ),
#     by = c("posteam", "player_name")
#   ) %>% 
#   distinct(posteam, player_name, adot, med_pts, targets) %>% 
#   arrange(med_pts) %>% 
#   ggplot(aes(x = adot, y = med_pts)) +
#   geom_point(aes(size = targets / 10), color = wr_fpts_cols$team_color, alpha = 0.75) +
#   geom_text_repel(aes(label = player_name), alpha = 4) + 
#   geom_smooth() +
#   labs(
#     title = "2025 WR Median Fantasy PPG vs ADOT (Min. 40 Targets)",
#     x = "ADOT",
#     y = "Median Fantasy PPG"
#   ) + 
#   theme(
#     legend.position = "none",
#     plot.title = element_text(hjust = 0.42)
#   )
```

#### **TE**

```{r te fan ppr}
# TE
df_te_rec = 
  df_szn_pbp %>% 
  filter(complete_pass == 1) %>% 
  group_by(posteam, receiver) %>% 
  summarize(n_rec = n()) %>% 
  drop_na(receiver) %>% 
  rename(player_name = receiver)

df_te_fpts = 
  df_fantasy %>% 
  left_join(df_te_rec) %>% 
  filter(n_rec >= n_rec) %>% 
  inner_join(
    fast_scraper_roster(2025) %>% 
    filter(position == "TE") %>% 
    select(gsis_id),
      by = c("player_id" = "gsis_id")
  ) %>% 
  select(posteam, week, player_name, player_display_name, fpts, fpts_ppr)

df_med_te = 
  df_te_fpts %>% 
  group_by(player_display_name, posteam) %>% 
  summarize(med_pts = median(fpts)) %>% 
  arrange(-med_pts) %>% 
  rowid_to_column("rank") %>% 
  mutate(player_name_og = player_display_name) %>% 
  filter(rank <= 20) %>% 
  unite("rank_name", rank, player_display_name, sep = ". ") %>% 
  arrange(med_pts) %>% 
  rename(player_display_name = player_name_og)

df_med_te_ppr = 
  df_te_fpts %>% 
  group_by(player_display_name, posteam) %>% 
  summarize(med_pts = median(fpts_ppr)) %>% 
  arrange(-med_pts) %>% 
  rowid_to_column("rank") %>% 
  mutate(player_name_og = player_display_name) %>% 
  filter(rank <= 20) %>% 
  unite("rank_name", rank, player_display_name, sep = ". ") %>% 
  arrange(med_pts) %>% 
  rename(player_display_name = player_name_og)

df_te_fpts_cols = 
  teams_colors_logos %>% 
  mutate(posteam = team_abbr) %>% 
  left_join(df_med_te, by = "posteam") %>% 
  group_by(med_pts, posteam) %>% 
  mutate(player_display_name = fct_reorder(as.factor(player_display_name), med_pts)) %>% 
  arrange(med_pts) %>% 
  drop_na(player_display_name)

df_te_fpts_ppr_cols = 
  teams_colors_logos %>% 
  mutate(posteam = team_abbr) %>% 
  left_join(df_med_te_ppr, by = "posteam") %>% 
  group_by(med_pts, posteam) %>% 
  mutate(player_display_name = fct_reorder(as.factor(player_display_name), med_pts)) %>% 
  arrange(med_pts) %>% 
  drop_na(player_display_name)

df_te_fpts %>% 
  left_join(df_med_te_ppr) %>% 
  group_by(med_pts, posteam) %>% 
  drop_na() %>% 
  mutate(rank_name = fct_reorder(as.factor(rank_name), med_pts)) %>% 
  ggplot(aes(x = fpts_ppr, y = rank_name)) +
  geom_boxplot(fill = df_te_fpts_ppr_cols$team_color, alpha = 0.75) +
  labs(
    title = glue::glue("2025 TE PPG (Min. {n_rec} Receptions)"), 
    x = "PPR Fantasy PPG", 
    y = "TE"
  ) + 
  theme(
    plot.title = element_text(hjust = 0.5),
    axis.text.y = element_text(size = 8)
  )

# fan_rb + fan_wr
# fan_qb + fan_te
# (fan_rb + fan_wr) / (fan_qb + fan_te) + plot_layout(heights = c(0.65, 0.35))
# (fan_rb_ppr + fan_wr_ppr) / (fan_qb + fan_te_ppr) + plot_layout(heights = c(0.65, 0.35))
```

:::

### **0.5 PPR**

::: panel-tabset

#### **QB**

```{r fan qb half ppr}
df_qb_fpts %>% 
  drop_na() %>% 
  ggplot(aes(x = fpts, y = rank_name)) +
  geom_boxplot(fill = df_qb_fpts_cols$team_color, alpha = 0.75) +
  labs(
    title = glue::glue("2025 QB PPG (Min. {n_att} Passing Attempts)"), 
    x = "Fantasy PPG", 
    y = "QB"
  ) + 
  theme(
    plot.title = element_text(hjust = 0.5),
    axis.text.y = element_text(size = 8)
  )
```

#### **RB**

```{r rb fan half ppr}
df_rb_fpts %>% 
  left_join(df_med_rb) %>% 
  drop_na() %>% 
  ggplot(aes(x = fpts, y = fct_reorder(as.factor(rank_name), -rank_og))) +
  geom_boxplot(fill = df_rb_fpts_cols$team_color, alpha = 0.75) +
  labs(
    title = glue::glue("2025 RB PPG (Min. {n_touches} Touches)"), 
    x = "Fantasy PPG", 
    y = "RB"
  ) + 
  theme(
    plot.title = element_text(hjust = 0.5),
    axis.text.y = element_text(size = 8)
  )
```

#### **WR**

```{r wr fan half ppr}
df_wr_fpts %>% 
  left_join(df_med_wr) %>% 
  left_join(df_wr_fpts_cols %>% select(player_display_name, team_color), by = "player_display_name") %>% 
  drop_na() %>% 
  # mutate(rank_name = ) %>% 
  # left_join(wr_fpts_cols %>% select(player_display_name, team_color)) %>% 
  ggplot(aes(x = fpts, y = fct_reorder(as.factor(rank_name), -rank_og))) +
  geom_boxplot(aes(fill = team_color), alpha = 0.75) +
  scale_fill_identity() +
  labs(
    title = glue::glue("2025 WR PPG (Min. {n_targets} Targets)"), 
    x = "Fantasy PPG", 
    y = "WR"
  ) + 
  theme(
    plot.title = element_text(hjust = 0.5),
    axis.text.y = element_text(size = 8)
  )
```

#### **TE**

```{r te fan half ppr}
df_te_fpts %>% 
  left_join(df_med_te) %>% 
  group_by(med_pts, posteam) %>% 
  drop_na() %>% 
  mutate(rank_name = fct_reorder(as.factor(rank_name), med_pts)) %>% 
  ggplot(aes(x = fpts, y = rank_name)) +
  geom_boxplot(fill = df_te_fpts_cols$team_color, alpha = 0.75) +
  labs(
    title = glue::glue("2025 TE PPG (Min. {n_rec} Receptions)"), 
    x = "Fantasy PPG", 
    y = "TE"
  ) + 
  theme(
    plot.title = element_text(hjust = 0.5),
    axis.text.y = element_text(size = 8)
  )
```

:::

:::
